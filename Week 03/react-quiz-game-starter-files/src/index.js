/*
  These starter files correspond to video 8: https://youtu.be/DHYiXjbUjuM. This includes the CSS &
  image files that we use in the video, along with placeholder JS files. If you download and use
  these, remember to run "npm install" first.
 */

import React from "react";
import ReactDOM from "react-dom";
import App from "./app";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.querySelector("#root")
);
